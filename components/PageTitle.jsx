import React from "react";

const PageTitle = ({ title }) => (
  <div className="py-12 flex justify-center items-center bg-black text-white w-full">
    <h1 className="">{title}</h1>
  </div>
);

export default PageTitle;
